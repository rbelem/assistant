***REMOVED*** Dev-time gitleaks hook.
***REMOVED*** Install once after cloning: `pip install pre-commit && pre-commit install`
***REMOVED*** The GitHub Action (.github/workflows/gitleaks.yml) is the authoritative gate;
***REMOVED*** this hook is fast feedback at commit time only.

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks

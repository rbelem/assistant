***REMOVED***!/usr/bin/env bash
***REMOVED*** bws-check — verify SM access token works.
***REMOVED*** Exit code 0 = OK, 1 = fail.
set -euo pipefail
dir="$(cd "$(dirname "$0")" && pwd)"
source "$dir/lib/bws.sh"
bws_check
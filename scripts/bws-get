#!/usr/bin/env bash
# bws-get — get a single SM secret value.
# Usage: bws-get KEY
# Reads BWS_ACCESS_TOKEN from env or ~/.config/bitw/config.
set -euo pipefail
dir="$(cd "$(dirname "$0")" && pwd)"
source "$dir/lib/bws.sh"
bws_get "${1?usage: bws-get KEY}"
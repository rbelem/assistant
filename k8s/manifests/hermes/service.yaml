apiVersion: v1
kind: Service
metadata:
  name: hermes-service
  namespace: hermes
  labels:
    app: hermes-agent
spec:
  ***REMOVED*** NodePort so host-level Caddy can proxy without k8s DNS resolution.
  ***REMOVED*** 30080 → OpenAI-compatible API server (hermes.REDACTED-DOMAIN/v1/*)
  ***REMOVED*** 30090 → web dashboard (hermes.REDACTED-DOMAIN/*)
  type: NodePort
  selector:
    app: hermes-agent
  ports:
    - name: api
      port: 8642
      targetPort: api
      nodePort: 30080
      protocol: TCP
    - name: dashboard
      port: 9119
      targetPort: dashboard
      nodePort: 30090
      protocol: TCP

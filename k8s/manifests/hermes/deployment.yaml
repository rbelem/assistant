apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-agent
  namespace: hermes
  labels:
    app: hermes-agent
    app.kubernetes.io/name: hermes-agent
    app.kubernetes.io/part-of: ai-agent-stack
spec:
  # Single replica — stateful agent, do not scale beyond 1
  replicas: 1
  strategy:
    type: Recreate # Avoid two pods fighting over the same PVC
  selector:
    matchLabels:
      app: hermes-agent
  template:
    metadata:
      labels:
        app: hermes-agent
        app.kubernetes.io/name: hermes-agent
    spec:
      containers:
        - name: hermes-agent
          # Nous Research Hermes Agent — self-hosted AI agent (Discord gateway)
          image: nousresearch/hermes-agent:latest
          imagePullPolicy: IfNotPresent
          args:
            - "gateway"
            - "run"
          ports:
            - name: api
              containerPort: 8642
              protocol: TCP
            - name: dashboard
              containerPort: 9119
              protocol: TCP
          envFrom:
            # Secrets created by Ansible from Bitwarden:
            # DISCORD_BOT_TOKEN, DISCORD_ALLOWED_USERS, OPENCODE_GO_API_KEY,
            # OPENROUTER_API_KEY, API_SERVER_KEY,
            # HERMES_DASHBOARD_BASIC_AUTH_USERNAME/_PASSWORD_HASH/_SECRET
            - secretRef:
                name: hermes-secrets
          env:
            - name: HERMES_HOME
              value: "/opt/data"
            - name: API_SERVER_ENABLED
              value: "true"
            - name: API_SERVER_HOST
              value: "0.0.0.0"
            - name: HERMES_DASHBOARD
              value: "1"
            - name: HERMES_DASHBOARD_HOST
              value: "0.0.0.0"
            - name: TZ
              value: "REDACTED-TZ"
          volumeMounts:
            - name: hermes-data
              mountPath: /opt/data
            - name: hermes-config
              mountPath: /opt/data/config.yaml
              subPath: config.yaml
          livenessProbe:
            httpGet:
              path: /health
              port: api
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: api
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "2"
              memory: 4Gi
      volumes:
        - name: hermes-data
          persistentVolumeClaim:
            claimName: hermes-data-pvc
        - name: hermes-config
          configMap:
            name: hermes-config
